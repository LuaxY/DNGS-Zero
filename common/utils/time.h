#ifndef TIME_H
#define TIME_H

#include <ctime>
#include <string>
#include <sstream>

std::string getCurrentTime(char separator = ':');
std::string getCurrentDate(char separator = ':');

#endif // TIME_H
